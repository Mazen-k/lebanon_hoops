import 'package:flutter/material.dart';

import '../app_shell.dart';

/// Lets the drawer (and pushed routes) switch the main bottom-nav tab.
final GlobalKey<AppNavigationShellState> appNavShellKey = GlobalKey<AppNavigationShellState>();
